import "./PresenceField.css";

export default function PresenceField({ children }) {
    return (
        <section className="aether-presence-field">
            {children}
        </section>
    );
}